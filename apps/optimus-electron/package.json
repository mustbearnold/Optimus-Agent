{
  "name": "optimus-electron",
  "productName": "Optimus Agent",
  "version": "0.1.0",
  "private": true,
  "description": "Optimus Agent Electron shell (T3-style host + renderer)",
  "main": "main.cjs",
  "scripts": {
    "start": "electron .",
    "dev": "bun scripts/dev.cjs",
    "dev:legacy-html": "OPTIMUS_ELECTRON_UI=legacy bun scripts/dev.cjs",
    "dev:ui": "OPTIMUS_ELECTRON_UI=react bun scripts/dev.cjs",
    "test": "bun test test/*.test.cjs",
    "test:e2e": "playwright test",
    "check": "bun -e \"new Function(await Bun.file('main.cjs').text()); new Function(await Bun.file('preload.cjs').text())\" && bun run test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "electron": "^43.2.0"
  }
}
