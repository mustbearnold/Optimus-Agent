[package]
name = "optimus-engineering"
description = "Durable, phased engineering runs in isolated git worktrees (ADR-0052)"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
# Matching a triage's component list against the profile's sensitive globs.
globset.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true
uuid.workspace = true

# Killing a timed-out step's whole process group, not just its leader.
[target.'cfg(unix)'.dependencies]
libc.workspace = true

[dev-dependencies]
tempfile.workspace = true
